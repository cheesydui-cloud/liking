# Changelog

每个版本必须先写本章节，再打 tag / 发 GitHub Release。

## v0.1.29 — 2026-09-13

换机器或怕数据丢了，只能重新录入用户、节点和证书。

### 改进
- 设置增加「备份」Tab：下载 gzip 快照 `.lkbak`（用户、套餐、节点、证书私钥、Cloudflare Token、Agent 令牌、面板设置）
- 上传后先预览数量再恢复；要填当前管理员密码，并勾选覆盖。会话作废，用备份里的管理员账号重新登录
- 迁机：新机器安装面板 → 恢复备份 → 域名变了就改面板 URL → 各服务器用原来的安装命令再执行一次（令牌没变）
- 备份不含登录会话；格式版本向前兼容，列对不上的会跳过或用库默认值

### 升级注意
- 只升面板即可。先下载一份备份再升级也行
- 回滚：`liking-upgrade --release v0.1.28`

## v0.1.28 — 2026-09-13

服务器公开地址只能手填 IP，域名托管到 Cloudflare 之后还得再敲一遍。

### 改进
- 服务器管理增加「从 CF 同步」：用设置里的 Cloudflare Token 拉取已托管域名，点选即可写成公开地址
- 指向这台机器 IP 的记录排在前面；橙云代理会提示（REALITY 需要 DNS only）
- 添加服务器时也可以从 CF 拉取，不必手输
- 通配符和 `_acme-challenge` 不会出现在列表里

### 升级注意
- 只升面板即可。设置里先保存 Cloudflare API Token（Zone 读取）
- 回滚：`liking-upgrade --release v0.1.27`

## v0.1.27 — 2026-09-13

复制 Mieru 链接没有节点名称，导入客户端后备注是空的。

### 修复
- `mierus://` 末尾带上节点名（`#HK-MIERU-8444`）。`profile=default` 仍是服务端配置名，不能改成节点名

### 升级注意
- 只升面板即可。重新复制链接或刷新订阅
- 回滚：`liking-upgrade --release v0.1.26`

## v0.1.26 — 2026-09-13

增加节点只有一个协议下拉和 dest，各协议该填的安全项看不到，REALITY 还默认伪装 Cloudflare，容易填成自己的域名。

### 改进
- 增加节点改成协议卡片：Vision 推荐，TLS 协议标需证书，Mieru / AnyTLS 标仅入口
- REALITY 可改 dest（微软 / 苹果等预设）、SNI、指纹、shortId、xver、spiderX，编辑时可轮换密钥。dest 不能指向本机
- XHTTP / Trojan / AnyTLS 可改 SNI、指纹、TLS 1.3、ALPN、拒绝未知 SNI；XHTTP 还有 path / mode / host
- SS2022 新建默认 256；Mieru 传输说明只当入口
- 内核配置写入 minVersion 1.3、ALPN、rejectUnknownSni；订阅带上指纹和 ALPN
- 默认 REALITY dest 改为 `www.microsoft.com:443`

### 升级注意
- 升面板后到服务器管理点一次同步，已有 TLS 节点会吃到 1.3 / 拒绝未知 SNI
- 已有 REALITY 节点的 dest 不变
- 回滚：`liking-upgrade --release v0.1.25`

## v0.1.25 — 2026-09-13

没有普通用户时，用户页直接显示空状态，管理员账号看不见。

### 修复
- 用户列表始终显示管理员（套餐 / 流量为 —，不可订阅或删除）
- 搜索「管理员」或用户名也能找到

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.24`

## v0.1.24 — 2026-09-13

密码单独占一侧栏，设置页把面板、证书叠在一起，管理员也不能改自己的登录名。

### 改进
- 设置改为 Tab：面板 / 证书 / 账号
- 密码收进设置的账号 Tab，侧栏不再单独列出
- 登录用户可改用户名（含管理员）；改用户名或密码需填写当前密码
- 用户侧「修改密码」改为设置，同样可改用户名

### 升级注意
- 只升面板即可。旧地址 `/password`、`/certs` 会跳到对应 Tab
- 回滚：`liking-upgrade --release v0.1.23`

## v0.1.23 — 2026-09-13

证书单独占一侧栏，又只能粘贴 PEM，没有域名的人没法给 Trojan / XHTTP / AnyTLS 配 TLS。

### 改进
- 证书收进设置，侧栏不再单独列出
- 三种签发：Cloudflare DNS 申请 Let's Encrypt（支持泛域名，域名不必指向面板）、自签、上传 PEM
- Let's Encrypt 到期前约 30 天自动续期；设置里保存 Cloudflare Token 和 ACME 邮箱（Token 不明文回显）

### 升级注意
- 只升面板即可。已有上传的证书仍可用
- 回滚：`liking-upgrade --release v0.1.22`

## v0.1.22 — 2026-09-13

浅色主题是暖米色加 terracotta，看起来不像纯白管理台。

### 改进
- 默认改为高清白：白底、细灰线、黑字、黑色主按钮
- 侧栏选中、表格、徽章去掉暖色铺底
- 深色改为锌黑配对，右上角仍可切换

### 升级注意
- 只升面板即可。已手动选过深色的人仍走深色
- 回滚：`liking-upgrade --release v0.1.21`

## v0.1.21 — 2026-09-13

首次安装成功只提示打开浏览器，管理员密码写在 journal 里，终端看不见。自定义 `--bootstrap-admin-password` 也没真正传到首次启动。

### 改进
- 安装成功后打印面板地址、用户名 `admin` 和密码
- 未指定密码时由安装脚本生成，并在服务启动时传入
- 重置密码后同样打印新密码

### 修复
- 一次性 bootstrap drop-in 改为启动后再删除，自定义首装密码生效

### 升级注意
- 已安装面板不受影响。新机器直接跑一键安装即可
- 已装机器更新安装脚本：`liking-upgrade update-script`
- 回滚：`liking-upgrade --release v0.1.20`

## v0.1.20 — 2026-09-13

服务器卡片上看不出这台机器现在忙不忙、已经走了多少流量、还剩多少。

### 改进
- 每台服务器展示实时上行 / 下行，页面约 5 秒刷新
- 已用流量按这台机器上的线路累计；可设上限，并显示剩余
- 总览节点表同步显示上行、下行、已用

### 说明
- 新 Agent 报网卡速率（排除 lo / docker / veth 等）
- 旧 Agent 仍可用 Xray 用户统计估算实时速率
- 已用流量不依赖升 Agent

### 升级注意
- 升面板即可看到已用流量；要看网卡实时速率，请重新执行该节点的 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.19`

## v0.1.19 — 2026-09-13

用户行上挤着订阅、清流量、改密、续期和换套餐，没法改用户名，也不能单独设流量上限。停用时还会把自定义流量上限清掉。

### 改进
- 行操作改为「订阅 / 编辑 / 停用 / 删除」，套餐和到期在表格里只读
- 编辑弹窗可改用户名、备注、套餐、到期、流量上限、登录密码，并清零已用流量
- 到期可用日期选择，并提供 +30 / +60 / +90 天

### 修复
- 只改启用状态时不再清空自定义流量上限
- 同一套餐保存时不再清零已用流量（换套餐仍会清零）

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.18`

## v0.1.18 — 2026-09-13

侧栏「节点管理」和「添加线路」对不上常见面板的叫法；新建时名称预填 HK-8443、端口预填 8443，容易和已用口撞上。

### 改进
- 侧栏和页面改为「服务器管理」；每台机器上「增加节点」
- 名称默认留空，保存时按协议和端口生成
- 端口不填则在 10000–59999 随机，并错开该服务器已用端口

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.17`

## v0.1.17 — 2026-09-13

SS2022 点「复制」提示「节点未填写公开地址」，Agent 其实已经上报了连接 IP。即使用户后来填了地址，旧的 `ss://` 还会把密钥里的 `/` `=` `:` 转成百分号，小火箭经常导不进去。线路也只听了 TCP。

### 修复
- 公开地址为空时，复制 / 订阅改用 Agent 上报的连接 IP；心跳时若公开地址仍空则自动填上（回环地址除外）
- SS2022 分享改为 SIP002：`ss://Base64URL(method:serverPSK:userPSK)@host:port#name`，密钥不再百分号编码
- SS2022 入站同时听 TCP 和 UDP

### 升级注意
- 只升面板即可。已在线的节点下次心跳会补上公开地址
- 套餐要勾选这条 SS2022 所在的节点，否则复制会提示还没有可用用户
- 回滚：`liking-upgrade --release v0.1.16`

## v0.1.16 — 2026-09-12

Mieru 会一会通、过一会又不通。节点没有公网 IPv6 时，mita 默认按 DNS 返回的第一个地址连；先返回 AAAA 就连不上，等解析顺序变了又通。面板每 30 秒重下发时还会对 Mieru 端口做一次空的 TCP 探测。

### 修复
- mita 配置固定 `dns.dualStack=PREFER_IPv4`，并设置 `mtu=1400`
- 配置没变且 mita 仍在 RUNNING 时不再 `stop`，也不再 TCP 拨测业务端口

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，只升面板不够
- 回滚：`liking-upgrade --release v0.1.15`

## v0.1.15 — 2026-09-12

Mieru 保存「TCP+UDP」时，占用检测把 mita 自己正在听的端口当成冲突，线路被自动停用，复制出来的端口上没人听。停用时还会删掉用户账号，再启用就换密码。

### 修复
- Agent 下发 mita 前先 `stop`，等自己的端口释放后再检测占用，改传输 / 改端口不会把线路自动关掉
- 下发后确认 mita 为 RUNNING，并用本机连接确认端口已在听
- 停用线路时保留该用户的 Mieru 用户名和密码，再启用不用重新导入

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，只升面板不够
- 已停用的 Mieru 线路请再启用一次，然后重新「复制」链接（若之前被自动停用，密码可能已换）
- 回滚：`liking-upgrade --release v0.1.14`

## v0.1.14 — 2026-09-12

Mieru 改端口后配置写了新口，mita 进程还在听旧口，客户端连的是空端口。小火箭默认走 UDP，线路却只开了 TCP。

### 修复
- Agent 下发 mita 配置后先 `stop` 再 `start`，端口和传输立刻生效
- 新建 Mieru 默认 TCP+UDP；分享链接在 UDP/BOTH 时为 `udp=1`（小火箭）
- Clash 订阅里 BOTH 按 UDP 导出

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，否则改端口仍不会重绑
- 已有 Mieru 线路请编辑一次传输为「BOTH」或「UDP」再保存
- 回滚：`liking-upgrade --release v0.1.13`

## v0.1.13 — 2026-09-12

线路「复制」被做成了订阅链接。小火箭认的是 `mierus://` 协议链接，例如 `mierus://user:pass@host?udp=1&port=39198&profile=default`。

### 修复
- 「复制」只复制协议链接：VLESS / Trojan / SS 仍是 `vless://` 等；Mieru 改为小火箭格式 `mierus://用户:密码@地址?udp=&port=&profile=default`
- 不再弹出订阅面板；复制失败时只展示这条协议链接

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.12`

## v0.1.12 — 2026-09-12

线路「复制」贴出的 `mieru://` 多数软件不认。真正通用的是 HTTP 订阅链接。

### 改进
- 点「复制」弹出导入面板，并自动复制通用订阅（`/api/sub/令牌`）：Clash / v2rayN / 小火箭 / sing-box 都能添加订阅
- 同时给出 Clash Meta 订阅；VLESS / Trojan / SS 仍可复制单节点 `vless://` 等
- Mieru / AnyTLS 不再把自定义单节点链接当作可导入格式，提示用 Clash Meta 订阅

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.11`

## v0.1.11 — 2026-09-12

节点管理里点「复制」，贴出来的是公钥、端口这类参数，不是 `vless://` 分享链接，代理软件导不进去。

### 修复
- 「复制」改为复制该线路一个可用用户的分享链接（`vless://` / `trojan://` / `ss://` / `anytls://` / `mieru://`）
- 没有用户或未填公开地址时给出明确提示；「参数」仍复制服务端字段，并说明不能当分享链接
- Mieru 单条链接多数客户端不认，提示改用用户页的 Clash 订阅

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.10`

## v0.1.10 — 2026-09-12

节点只装了 Xray 时，创建 Mieru / AnyTLS 会被面板拦住，而且 Agent 也不会去装 mita。

### 修复
- 允许在尚未上报 mita / sing-box 时创建对应线路，配置会下发给节点
- Agent 第一次下发时从 GitHub 安装缺失内核（mita 3.36.1、sing-box 1.14.0、Xray），装好后回传已装内核
- 创建表单不再拦截，只提示「将自动安装」；装失败时节点卡片显示下发错误

### 升级注意
- 必须重新执行节点上的 Agent 安装命令，否则仍不会自动装 mita
- 节点需要能访问 GitHub；可设环境变量 `LIKING_GITHUB_PROXY`
- 回滚：`liking-upgrade --release v0.1.9`

## v0.1.9 — 2026-09-12

侧栏不再拆「服务器 / 入站」，改成一页「节点管理」：一台机器下面挂线路。

### 改进
- 新页面「节点管理」：节点卡片 + 嵌套线路表，在本机上添加/编辑/停用线路
- 侧栏「节点」分组只保留 节点管理、证书；旧地址 `/servers`、`/inbounds` 自动跳转
- 总览、套餐文案跟着改成「节点 / 线路」，避免再在两个菜单之间来回跳
- 添加线路时节点已锁定，端口仍可自定义（默认建议 8443，避开 443）

### 升级注意
- 面板 UI 变更，升到 v0.1.9 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.8`

## v0.1.8 — 2026-09-12

对照妙妙屋的操作面板，把 liking 管理端从金纸衬线风改成紧凑后台。

### 改进
- 侧栏分组（节点 / 业务 / 系统），列表优先，新建和编辑都在弹窗里完成
- 去掉 Cormorant 衬线与金纸底纹；Fira Sans 全文，terracotta 强调色，深浅色都可用
- 用户、套餐、入站、服务器、证书的创建表单移入对话框；空状态带主操作按钮
- 登录改为居中卡片；总览数字与套餐/服务器卡片改为信息密度更高的排版

### 升级注意
- 面板 UI 变更，升到 v0.1.8 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.7`

## v0.1.7 — 2026-09-12

对照妙妙屋的日常操作，把开户、续期、订阅和入站参数抄进 liking（不搬偷自己、Nginx、TG、十二套模板、一人多套餐）。

### 新增
- 创建用户可留空密码：服务端随机生成并返回，面板自动复制
- 用户续期 `extend_days`（+30 / +60 / +90），只改到期日，不清流量
- 用户搜索、按套餐筛选、流量进度条、到期标记、订阅弹窗（二维码 + Clash / sing-box 一键导入）
- 入站按服务器筛选；可查看/复制 REALITY 公钥、short_id、dest、SS2022 PSK 等参数（不含私钥）
- 套餐卡片展示流量、周期、关联节点和用户数；不勾选节点时二次确认「全部节点」
- 服务器改为卡片：在线状态、已装内核、入站条数、心跳、安装命令
- 总览增加用户数、套餐数、已用流量；「我的订阅」带流量条和导入按钮

### 升级注意
- 面板 UI 变更，升到 v0.1.7 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.6`

## v0.1.6 — 2026-09-12

真机验收：套餐无法选节点、入站默认 443 被占用会拖垮整机、HTTP 下复制订阅失败、用户无法改套餐。

### 修复
- 创建/编辑套餐改为勾选**节点**（服务器）；不选表示全部节点，节点上后加的线路自动进入套餐
- 入站端口默认为 8443，可自定义 1–65535，可编辑已有入站的端口/名称
- 某一端口被占用时只停用该入站，其它线路继续监听；缺失 mita / sing-box 不再让 Xray 下发失败
- Agent 上报已安装内核；订阅只包含节点上实际能跑的协议
- HTTP 打开面板时复制订阅/安装命令不再静默失败，并弹出可手动复制的链接
- 用户列表可改绑套餐；创建用户后直接给出订阅链接

### 改进
- 入站表单显示该节点已用端口，并按空闲口自动建议
- 占用端口的入站会自动停用，改端口后可再启用

### 升级注意
- 升到 v0.1.6 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.4`（会失去套餐选节点与端口修复）

## v0.1.5 — 2026-09-12

未单独打 GitHub tag，内容并入 v0.1.6。

### 修复
- 创建/编辑套餐改为勾选**节点**（服务器），不再只显示入站名称小标签
- 勾选节点后，该节点上后加的线路会自动进入套餐
- 套餐列表和开用户时能看到套餐绑了哪些节点

## v0.1.4 — 2026-09-12

真机验收：Xray Stats API 路由配成 freedom，自己连自己把 fd 打到九万。

### 修复
- API 入站改为路由到 `api` 模块，不再走 `freedom`（原先 `127.0.0.1:10085` 会环回，几十秒内撑爆 1.9G 机器）

### 升级注意
- 升到 v0.1.4 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.3`（需对应 Release 资产；不建议回滚，会再出现内存风暴）

## v0.1.3 — 2026-09-12

真机验收：Xray 流量采集把 1.9G 机器吃满。

### 修复
- `xray api statsquery` 加 4 秒超时，避免卡死的采集进程把 API 连接堆在 10085
- 流量采集改为 30 秒一次，且同时只跑一条
- 拉起 Xray / sing-box 时设置 `GOMEMLIMIT=256MiB`，避免 Go 堆 + 透明大页把整机换爆

### 升级注意
- 升到 v0.1.3 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.2`（需对应 Release 资产）

## v0.1.2 — 2026-09-12

真机验收：配置没变时不再每 30 秒重启 Xray。

### 修复
- 周期巡检下发若配置未变且内核仍在跑，则跳过重启（原先 VLESS 会每 30 秒闪断）
- 相同的下发失败只记一次日志，避免刷 journal
- 下发失败文案去掉多余的 `apply rejected:` 前缀
- Agent 安装时清理 v0.1.0 留在 `/var/lib/liking/xray.log` 的残留日志

### 升级注意
- 升到 v0.1.2 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.1`（需对应 Release 资产）

## v0.1.1 — 2026-09-12

真机验收修复：配置下发失败可见、端口占用不再抢已有服务、Xray 日志不再刷盘。

### 修复
- 节点配置下发失败会写入服务器 `last_error`，总览和服务器页直接显示，不再只打日志
- 创建/修改入站会同步等待下发结果，失败时返回 `apply_error`
- Agent 启动前检测公网端口；若 443 等已被占用则拒绝下发并回滚到上一份可用配置（避免和本机其它服务抢端口）
- Xray 关闭 access 日志，避免 Stats API 把 `xray.log` 刷到数 MB
- 并发下发合并为每台机器一条队列，减少 Xray 反复重启和僵尸进程
- Agent 数据目录改为 `/var/lib/liking/agent`，不再和面板 `panel.db` 混在同一目录

### 改进
- 登录页与卡片质感加强；443 端口给出占用提示
- Agent systemd 带完整 PATH；重装安装脚本会 restart 而不是只 enable
- 面板 `install.sh` / `liking-upgrade` 替换二进制后会 `systemctl restart`（原先 `enable --now` 不会重启已在跑的进程）
- 某一内核缺失时仍下发其它内核（例如没有 mita 时 VLESS 照常监听）

### 升级注意
- 升到 v0.1.1 后请在节点上重新执行面板里的 Agent 安装命令，以更新二进制和数据目录
- 入站默认仍是 443；本机已有 Nginx / 其它面板占用 443 时请改用 8443
- 回滚：`liking-upgrade --release v0.1.0`（需对应 Release 资产）

## v0.1.0 — 2026-09-12

第一版面板：主控 + 节点 Agent，深色金质管理台，一键安装与升级。

### 新增
- 管理员登录、服务器列表、反向 WSS 纳管节点
- 七组入站：VLESS+REALITY、VLESS+REALITY+Vision、VLESS+XHTTP+TLS、Trojan+TLS、SS2022、AnyTLS+TLS、Mieru
- 用户 / 套餐（一人一套餐）；到期或超量从内核配置摘掉客户端
- 订阅：Clash Meta YAML、sing-box JSON、URI（base64）
- 链式转发：入口 → 另一台落地；Mieru / AnyTLS 不能当链式落地
- 面板一键安装、`liking-upgrade` 升级、`liking-uninstall` 卸载；支持 `--from-dir` 离线安装
- 节点从面板 `/v1/install-agent` 下载 Agent，不走 GitHub；`/v1/agent-bin` 带 `X-SHA256`，支持 HEAD

### 改进
- 默认深色电影金质 UI，浅色为暖纸色例外主题；字体打进二进制，不依赖 Google Fonts
- SQLite WAL；删除服务器会断开在线 Agent
- 列表接口不再返回节点 token

### 升级注意
- 默认监听 `:8899`
- 节点需自行安装内核到 PATH：`xray`、AnyTLS 用 `sing-box`（≥1.12）、Mieru 用 `mita`
- 首次安装会打印 admin 密码；也可用 `--bootstrap-admin-password`
