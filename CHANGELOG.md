# Changelog

每个版本必须先写本章节，再打 tag / 发 GitHub Release。

## v0.1.59 — 2026-09-15

点左侧栏会先闪一下「还没有实例 / 没有节点」。列表先用上次结果，没数据时只出骨架。

### 改进
- 节点、实例、用户、套餐、转发、中转、总览记住上次列表。再点侧栏先画出已有数据，不再先当空列表
- 第一次还没拉到数据时只显示骨架，空状态要等请求结束才出现
- 进面板就预取实例和节点。总览里的实例列表写入同一份缓存。退出登录清掉

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.58`

## v0.1.58 — 2026-09-15

点左侧栏会整页重挂，跳转被打断。侧栏改为常驻。

### 改进
- 登录后侧栏和顶栏只挂一次，点选项只换中间内容，不再把整页壳卸掉重挂
- 关掉 React Router 的 `startTransition`，点击后立刻切页
- 窄屏抽屉改成路由变了再关，避免点链接的同时关菜单把跳转打断

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.57`

## v0.1.57 — 2026-09-15

审计修复：公网不信伪造 IP、备份要密码、节点复制用当前账号、套餐必须勾选节点。

### 改进
- 公网 :8899 不再信任 `X-Forwarded-For` / `X-Real-IP`。只有本机回环（nginx）才会改客户端 IP
- 备份改为 POST，要当前密码或备份密码。去掉 GET `/api/backup`
- 节点「复制」给出当前登录账号的链接，不再带订阅令牌
- 改密码会使其它会话立刻失效。开启两步验证要先填当前密码
- 响应加 `X-Frame-Options: DENY`、`nosniff` 和 CSP
- socks / Xray API / sing-box API 端口挪到 61000 / 60085 / 60190，不再占用用户端口池
- Mieru 用户名按入站区分（`u{用户}.i{节点}`），流量统计不再串线
- 套餐不勾选节点就是 0 个节点，创建必须勾选
- 节点卡片流量标「全站」。名片密码只显示一次，复制或登录后清除，可重置再复制
- 流量写入走事务。配额刚超才重新下发，不再每次上报都 provision
- 不能再创建第二个管理员。JSON 请求上限 1MB。到期日按上海时区当天结束

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 旧套餐如果当初不勾选表示「全部节点」，升级后会变成 0 个节点，要进套餐重新勾选
- 已在线的 Mieru 用户下次下发会改用户名，流量按新身份统计
- 回滚：`liking-upgrade --release v0.1.56`

## v0.1.56 — 2026-09-15

用户页每个节点显示已用流量；中转节点带标签。

### 改进
- 我的订阅「可用节点」协议和复制之间显示当前账号在该节点的已用流量
- 流量只统计当前登录用户，不展示节点上其他人的用量，也不改计费
- 通过中转创建的节点，名称后面用青绿小字标「中转」，和实例直连节点区分
- `/api/me/nodes` 返回当前用户的 `used_up` / `used_down` 和 `line_kind`

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.55`

## v0.1.55 — 2026-09-15

用户页每个节点可复制单条链接。

### 改进
- 我的订阅「可用节点」每一行右边加「复制」
- 复制的是当前登录账号自己的节点链接。管理员用自己的身份，不占用用户额度
- 浏览器拦剪贴板时弹出链接，可手动选
- 用户页节点列表带上分享 URI，点复制不用再走管理页

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.54`

## v0.1.54 — 2026-09-15

登录页去掉「登录面板」，加上「记住我」。

### 改进
- 品牌下面不再显示「登录面板」
- 密码和登录按钮之间加「记住我」。勾选后同一浏览器会话保留 30 天，再打开不用填
- 不勾选仍是 12 小时。退出登录后还是要重新输入
- 登录按勾选设置 cookie 和会话过期时间，不把密码写进浏览器

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.53`

## v0.1.53 — 2026-09-15

管理员用户页加「中转」。链式从已有节点克隆入口，出口可贴 vless / ss / trojan。

### 改进
- 用户页侧栏「中转」只给管理员。普通用户看不到，直接打开也会回到订阅
- 链式会新建一条入口，原来的节点不动。出口可选面板节点，或粘贴 vless / ss / trojan（也可以 socks5）
- 卡片可探测入口到出口的 TCP 延迟。旧 Agent 立刻提示升级，不会标成正常
- 管理页转发保存时不再把 vless 出口改写成 socks5

### 升级注意
- 升面板。同机 Agent 会随 `liking-upgrade` 重启，带上延迟探测
- 其它机器要点「一键升级 Agent」才能探测。日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.52`

## v0.1.52 — 2026-09-14

管理员可从总览右上角切到用户页，复制包含全部节点的自己的订阅。

### 改进
- 总览右上角加「用户页」，侧栏换成订阅和设置。点「返回管理」回到面板
- 管理员订阅包含全部节点，用自己的身份，不占用用户额度
- 节点上「复制」仍是给用户用的。没有绑定用户时会提示走右上角用户页

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 打开用户页时会给管理员写入各节点的独立 UUID，并下发到在线机器。日本线 Agent 不升二进制
- 回滚：`liking-upgrade --release v0.1.51`

## v0.1.51 — 2026-09-14

弹窗右上角关闭按钮不再被挤出框外。标题栏固定，只有内容区滚动。

### 改进
- 关闭按钮改成和卡片上复制/编辑一样的小方按钮，不再被幽灵按钮的内边距撑破
- 标题太长会省略，不会把 × 挤出弹窗右边
- 标题和底栏固定，只有中间内容滚动。方形幽灵按钮的内边距一并修好

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.50`

## v0.1.50 — 2026-09-14

节点页改成和套餐、实例、用户同一套三列卡片。仍按实例分组。

### 改进
- 节点列表改成一行三张卡片，悬停上浮。实例名、在线/故障和节点数仍作为分组标题
- 卡片上显示协议、端口、流量、核心。正常左边绿条，故障红条，停用/离线灰条
- 主操作是复制节点链接，左下角打开参数。编辑、停用、删除在右上角
- 搜索、状态筛选、按实例过滤和增加节点弹窗不变。链式和端口中转仍只在转发里

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.49`

## v0.1.49 — 2026-09-14

用户页改成和套餐、实例同一套三列卡片。账号少的时候不再拉出一条空表格。

### 改进
- 用户列表改成一行三张卡片，悬停上浮，和套餐、实例同一套样式
- 卡片上显示套餐、到期、额度、计费，下面跟流量条。到期日期过期标红
- 到期或超量左边红条，停用灰条。主操作是复制名片，订阅放在左下角
- 编辑、流量、停用、删除仍在右上角。搜索、套餐筛选和将满/已到期不变

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.48`

## v0.1.48 — 2026-09-14

机器页改叫实例。名称统一海军蓝。各页去掉标题下说明。侧栏加大。用户列表不显示管理员。套餐编辑整理过。

### 改进
- 侧栏「服务器」改成「实例」，页面标题、添加/编辑和总览列表一并改名。分组名仍是服务器
- 实例名称统一海军蓝，不再按机器随机着色。侧栏标题、分组、导航和底部账号字号一起加大
- 总览、实例、用户、套餐、转发、流量、设置页去掉标题下说明。用户列表不再显示 admin
- 套餐筛选下拉收短，和搜索框一样不再拉满。套餐编辑：名称/流量/计费一行，计费改切换，选中节点用左侧竖条，倍率不再被拉宽

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.47`

## v0.1.47 — 2026-09-14

套餐改成和服务器一样的三列卡片。服务器卡片改成已用流量、到期/月重置、彩色名称和核心，并支持编辑与推送核心。搜索框统一缩短。

### 改进
- 套餐页改成一行三张卡片，悬停上浮，和服务器页同一套样式
- 服务器卡片内存改成已用流量；补到期时间和每月流量重置日。名称按机器着色加大，核心着色加粗。元信息只留版本、心跳和核心；上下行实时速率分色
- ⋯ 里「端口区间」改成「编辑」，可改名称、地址、端口区间、流量上限、到期和重置日。另加「推送核心」：下拉推送或卸载 Xray / sing-box / mita
- 各页搜索框统一缩短，不再拉满工具栏

### 升级注意
- 升面板即可看新卡片。同机 Agent 会随 `liking-upgrade` 重启
- 升上来后会给 servers 表加 `expires_at` / `traffic_reset_day`，已有机器为空
- 「推送核心」需要 Agent 带 `cores` 能力。其它机器请在服务器页一键升级 Agent；旧 Agent 会提示版本过低
- 回滚：`liking-upgrade --release v0.1.46`

## v0.1.46 — 2026-09-14

服务器卡片改小，一行三张、悬停上浮。每台机器可设端口区间，新节点不填端口时在这个区间随机。

### 改进
- 服务器页改成一行三张小卡片，悬停上浮，不再整行拉满
- 每台服务器可设端口区间，默认 10000–59999。添加时能填，卡片左下角和 ⋯ 菜单也能改
- 新节点不填端口时在该机区间内随机，躲开已用端口。手填端口不受限，已有节点不变
- 节点和转发表单会提示当前机器的随机区间

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 升上来后会给 servers 表加 `port_min` / `port_max`，已有机器默认 10000–59999
- 回滚：`liking-upgrade --release v0.1.45`

## v0.1.45 — 2026-09-14

国内机器装 Agent：安装框可勾选走 GitHub 镜像拉内核。面板在国外时 Agent 本体仍从本面板下载。

### 改进
- 一键安装框加「国内机器」。勾选后命令带 `--gh-proxy https://gh-proxy.com/`，镜像地址可改，勾选会记住
- 安装脚本把镜像写入 `/etc/liking/gh-proxy` 和 systemd 环境。第一次下发节点时 Xray / sing-box / mita 走镜像
- Agent 本体仍从本面板 `/v1/agent-bin` 下载，不走 GitHub。没勾选时行为和以前一样

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 已经装好的国内机器要再复制一次带勾选的安装命令执行，内核镜像才会生效
- 回滚：`liking-upgrade --release v0.1.44`

## v0.1.44 — 2026-09-14

去掉 AI 风：冰蓝灰渐变、毛玻璃、大标题和胶囊按钮。信息架构不变。

### 改进
- 页面底改成纸色实底，卡片、侧栏、顶栏、弹窗和 ⋯ 菜单用白底 1px 边，不再半透明毛玻璃
- 页标题收成 18 / 600；主按钮、幽灵按钮、行内操作改成小圆角矩形，筛选和 Tab 改成文字加下划线
- 侧栏实底，当前项用左边条而不是胶囊。正文字体优先系统黑体，数字和地址才用等宽
- 海军蓝只留在主按钮和当前导航条；深色改成实底石板，不再玻璃叠海军

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.43`

## v0.1.43 — 2026-09-14

节点从服务器管理拆出来：侧栏先节点后服务器；链式路径按跳显示状态。

### 改进
- 侧栏「服务器」组里先「节点」后「服务器」。`/nodes` 是节点，`/servers` 是机器；总览告警进服务器页
- 节点按服务器分组。增加节点先下拉选服务器再选协议；离线也能选，下发会等上线。链式和端口中转只在转发里
- 节点状态用已有数据：正常 / 停用 / 离线 / 故障 / 未安装。没流量也可以是正常；停用不标红
- 服务器一机一卡：未安装主按钮复制安装命令，在线看指标，离线看错误。卡片链到该机节点，不再嵌节点表
- 链式路径每一跳带落地状态；SK5 只显示地址，不假装正常。本版不做 TCP 探测

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.42`

## v0.1.42 — 2026-09-13

面板改成冰蓝灰毛玻璃：海军蓝主按钮，删除一律红底白字。

### 改进
- 页面底改成冰蓝灰渐变；侧栏、顶栏、卡片、弹窗和 ⋯ 菜单用半透明毛玻璃
- 页标题加大加粗（手机 22 / 桌面 28）；主按钮和选中筛选改成海军蓝胶囊
- 删除、清零、卸载确认改成红底白字；⋯ 里的删除是红字。停用仍是普通操作
- 深色模式改成海军底上的玻璃，不再用锌黑

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.41`

## v0.1.41 — 2026-09-13

用户行一键复制名片：网址、账号、密码、到期时间和订阅。

### 改进
- 用户列表复制按钮和 ⋯「复制名片」写出面板地址、账号、密码、到期、套餐、流量和订阅链接
- 新建、改密、批量开户会保存登录密码，之后随时能复制完整名片；登录仍核验哈希，`/me` 不返回密码
- 创建用户或重设密码成功后直接把名片放进剪贴板
- 本版之前开的账号没有保存的密码，名片里会暂时没有密码这一行，编辑用户重设一次即可

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启。升上来后会给 users 表加 `password_plain` 列
- 回滚：`liking-upgrade --release v0.1.40`

## v0.1.40 — 2026-09-13

增加 SOCKS5 入站；转发改成下拉，链式可自行加跳。

### 改进
- 增加节点可选 SOCKS5：用户名密码，走 sing-box，可 UDP，可当链式落地
- 转发空列表不再放筛选 Tab；类型和列表筛选改成下拉
- 链式转发可增加跳数（最多 5 跳），每跳本面板节点或 SK5；入口机用 Xray dialerProxy / sing-box detour 串起来
- 删除仍被中间跳指向的落地会拦截

### 升级注意
- 只升面板即可。同机 Agent 会随 `liking-upgrade` 重启
- 回滚：`liking-upgrade --release v0.1.39`

## v0.1.39 — 2026-09-13

Clash / sing-box 订阅可绑定分流；业务下增加转发表；套餐节点列改成数量和机器名。

### 改进
- 设置 → 订阅：极简 / 均衡 / 完整 / 自定义，默认均衡。规则集来自 MetaCubeX（与妙妙屋同一套）。广告拦截默认丢弃，私有网络和国内默认直连
- Clash Meta 订阅带 rule-providers 与分组（节点选择 / 自动选择 / 回落）；sing-box 订阅带 route.rule_set。通用 URI 仍只含节点
- 业务 → 转发：入口 → 落地。三种落地：本面板节点、SK5、IP+端口。端口中转不进订阅和套餐勾选
- 套餐列表「节点」列显示数量和机器名，悬停看出节点名

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.38`

## v0.1.38 — 2026-09-13

用户、套餐、流量、设置、订阅与服务器管理同一套交互：行内用图标按钮，不常用进 ⋯，可复制的字不再做成隐形按钮。

### 改进
- 用户：名称旁只标到期 / 超量 / 将满 / 停用；流量条只展示；铅笔改资料、链接看订阅；流量、停用、删除进 ⋯
- 套餐铅笔编辑、删除进 ⋯；证书续期和删除进 ⋯；域名、订阅链接、总览地址可直接选中复制
- 流量「导出 CSV」、总览「管理 / 明细」改成带描边的按钮；订阅主按钮仍是「复制 Clash 订阅」

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.37`

## v0.1.37 — 2026-09-13

服务器管理按身份 → 指标 → 节点表分层；不常用操作进 ⋯。公开地址可直接选中复制，不再做成隐形按钮。

### 改进
- 每台机器卡片：名称/状态/地址在上，上行/下行/负载/内存其次，流量条和元信息在下，节点表单独一块
- 同步、安装命令、升级、卸载、删除等收进 ⋯；节点行复制/编辑改成带描边的图标按钮；「增加节点」仍是文字按钮
- 公开地址是可选中复制的等宽字，旁边铅笔按钮改地址；流量条只展示，改上限在 ⋯ 里

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.36`

## v0.1.36 — 2026-09-13

可点击件不再长得像静态字：按钮有灰底、描边和圆角；侧栏字加大加粗；分割线加深。

### 改进
- 改、同步、从 CF 同步、安装命令、复制、编辑、更多、增加节点等行内操作改成浅灰底 + 1px 描边 + 4px 圆角，一眼能看出可点
- 筛选标签（全部 / 在线 / 离线 / 可升级）改成灰底块，选中黑底白字，不再用下划线文字
- 侧栏字号 15px、字重 Medium，当前项 Semibold + 浅灰底，左侧墨脊保留
- 分割线和容器边框保持 1px，颜色从 `#E8E8E8` 加深到 `#C4C4C4`

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.35`

## v0.1.35 — 2026-09-13

面板按「运行图」重做：状态用 3px 脊而不是圆点，总览用大号在线台数，列表用表不用卡。

### 改进
- 总览：巨大「在线 / 共 N 台」+ 3px 状态脊；告警改成左脊行；服务器表每行带在线 / 故障 / 离线脊
- 字体 IBM Plex Sans / Mono，真黑 `#000000`，圆角不超过 2px，无阴影。侧栏当前项是墨脊，不再用灰圆角条
- 服务器改成带脊的表块 + 内嵌节点表；套餐、流量、订阅去掉 SaaS 卡；筛选改成文字下划线
- 登录页白底细线框；标志改成一对高低竖条。青绿 `#0F766E` 只给「在线」，主按钮仍是黑底白字

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.34`

## v0.1.34 — 2026-09-13

按 ui-ux-pro-max、frontend-design、Vercel web-design-guidelines 重跑界面：去掉 SaaS 六宫格和假黑 `#111`，总览改成状态条，在线改成链路青绿。

### 改进
- 总览：告警在最上面；六个 KPI 卡收成一条状态栏；「开始使用」做到有用户才消失
- 在线状态用链路青绿，不再用通用翠绿；主按钮仍是黑底白字
- 服务器筛选补「离线」
- 跳到内容、主题色、44px 手机点按、搜索框标签、弹层 `overscroll-behavior`、加载文案用 `…`

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.33`

## v0.1.33 — 2026-09-13

面板功能齐了，但扫视费劲：服务器卡片六个操作并列，总览把机器叫成节点，旧 Agent 一点升级会空等三分钟。

### 改进
- 服务器操作收成「同步 / 安装命令 / 更多」；升级、轮换令牌、卸载、删除进菜单。低于 0.1.32 的 Agent 一键升级/卸载立刻提示用安装命令重装，不再空等
- 总览 KPI 改为服务器 / 在线 / 节点 / 用户 / 计费 / 今日；告警可点进对应页；空面板给出 6 步开始使用
- 公告进顶栏；流量「计费」卡加重；用户进度条标 80%；将满 / 已到期可筛选；订阅页主按钮复制 Clash
- 补琥珀警告色。手机主操作吸底，节点表改卡片

### 升级注意
- 只升面板即可。旧 Agent 仍要在机器上再跑一次安装命令
- 回滚：`liking-upgrade --release v0.1.32`

## v0.1.32 — 2026-09-13

面板缺运维能力：Agent 不能远程升级/卸载，流量超量不断连，备份不能加密，管理员没有 2FA。

### 改进
- 服务器管理一键升级 / 一键卸载 Agent（在线机器通过 WSS 拉面板 `/v1/agent-bin`，校验 sha256 后替换并重启；同机卸载不删面板和数据库）。可轮换 Agent 令牌
- `liking-upgrade` 升级前备份 `panel.db`（保留 7 份），并重启本机 `liking-agent`
- 面板可选 HTTPS（设置里选证书后重启 liking-server）；订阅带 `Subscription-Userinfo` 和 `Profile-Update-Interval`
- 时区默认 Asia/Shanghai；公告；管理员 IP 白名单；管理员 TOTP 两步验证；操作审计和在线会话只读
- 备份可加密为 `.lkb1`，定时备份保留 N 份；流量页 CSV / 本月 / 计费·原始·网卡三行数；套餐按节点设倍率；用户批量开户、流量重置日、80% 预警
- 服务器流量上限真正生效（超量停入站并摘掉客户端）；内核崩溃 watchdog、apply 前 `xray -test` / `sing-box check`；Clash 订阅 DNS 用 conservative fake-ip
- Agent 上报磁盘 / 内存 / 负载 / 连接数 / 正在跑的内核

### 升级注意
- 升面板。同机 Agent 会随 `liking-upgrade` 一起重启；其它机器在服务器管理点「一键升级」，或再跑一次安装命令
- 选择面板证书后必须重启 `liking-server` 才会变成 HTTPS
- 回滚：`liking-upgrade --release v0.1.31`

## v0.1.31 — 2026-09-13

Agent 只采 Xray 用户流量，AnyTLS / Mieru 不计费；日表有数据但管理端看不到，用户进度条也不按双向计费。

### 改进
- Agent 同时采集 Xray、sing-box（AnyTLS，Clash API）和 mita（Mieru）用户流量，每 5 秒上报
- 新增「流量」页：按日柱状图、用户 / 节点排行；总览有近 14 日和今日
- 用户已用按套餐单向 / 双向和节点倍率显示，和超量下线一致；点流量可看该用户每日和节点明细
- 服务器管理里每个节点显示累计流量；用户端订阅页进度条与计费对齐

### 升级注意
- 升面板和 Agent。同一台机器跑 `liking-upgrade` 即可；其它节点再执行一次安装命令，AnyTLS / Mieru 才会开始计数
- 升完后到服务器管理点一次同步，已有 AnyTLS 节点会打开本机 Clash API
- 回滚：`liking-upgrade --release v0.1.30`

## v0.1.30 — 2026-09-13

套餐表单预填了名称和 100 GB / 30 天，勾选的又是服务器而不是节点。

### 改进
- 套餐不再设周期；到期在用户上设置。新套餐默认不按天清零流量
- 名称、流量默认留空；流量留空即不限
- 「包含节点」按服务器分组列出全部节点（名称、协议、端口），可全选本机 / 全选当前。不选 = 全部节点
- 套餐卡片展示节点名，不再展示服务器名

### 升级注意
- 只升面板即可。已有按服务器勾选的套餐仍有效；再保存会改成按节点
- 回滚：`liking-upgrade --release v0.1.29`

## v0.1.29 — 2026-09-13

换机器或怕数据丢了，只能重新录入用户、节点和证书。

### 改进
- 设置增加「备份」Tab：下载 gzip 快照 `.lkbak`（用户、套餐、节点、证书私钥、Cloudflare Token、Agent 令牌、面板设置）
- 上传后先预览数量再恢复；要填当前管理员密码，并勾选覆盖。会话作废，用备份里的管理员账号重新登录
- 迁机：新机器安装面板 → 恢复备份 → 域名变了就改面板 URL → 各服务器用原来的安装命令再执行一次（令牌没变）
- 备份不含登录会话；格式版本向前兼容，列对不上的会跳过或用库默认值

### 升级注意
- 只升面板即可。先下载一份备份再升级也行
- 回滚：`liking-upgrade --release v0.1.28`

## v0.1.28 — 2026-09-13

服务器公开地址只能手填 IP，域名托管到 Cloudflare 之后还得再敲一遍。

### 改进
- 服务器管理增加「从 CF 同步」：用设置里的 Cloudflare Token 拉取已托管域名，点选即可写成公开地址
- 指向这台机器 IP 的记录排在前面；橙云代理会提示（REALITY 需要 DNS only）
- 添加服务器时也可以从 CF 拉取，不必手输
- 通配符和 `_acme-challenge` 不会出现在列表里

### 升级注意
- 只升面板即可。设置里先保存 Cloudflare API Token（Zone 读取）
- 回滚：`liking-upgrade --release v0.1.27`

## v0.1.27 — 2026-09-13

复制 Mieru 链接没有节点名称，导入客户端后备注是空的。

### 修复
- `mierus://` 末尾带上节点名（`#HK-MIERU-8444`）。`profile=default` 仍是服务端配置名，不能改成节点名

### 升级注意
- 只升面板即可。重新复制链接或刷新订阅
- 回滚：`liking-upgrade --release v0.1.26`

## v0.1.26 — 2026-09-13

增加节点只有一个协议下拉和 dest，各协议该填的安全项看不到，REALITY 还默认伪装 Cloudflare，容易填成自己的域名。

### 改进
- 增加节点改成协议卡片：Vision 推荐，TLS 协议标需证书，Mieru / AnyTLS 标仅入口
- REALITY 可改 dest（微软 / 苹果等预设）、SNI、指纹、shortId、xver、spiderX，编辑时可轮换密钥。dest 不能指向本机
- XHTTP / Trojan / AnyTLS 可改 SNI、指纹、TLS 1.3、ALPN、拒绝未知 SNI；XHTTP 还有 path / mode / host
- SS2022 新建默认 256；Mieru 传输说明只当入口
- 内核配置写入 minVersion 1.3、ALPN、rejectUnknownSni；订阅带上指纹和 ALPN
- 默认 REALITY dest 改为 `www.microsoft.com:443`

### 升级注意
- 升面板后到服务器管理点一次同步，已有 TLS 节点会吃到 1.3 / 拒绝未知 SNI
- 已有 REALITY 节点的 dest 不变
- 回滚：`liking-upgrade --release v0.1.25`

## v0.1.25 — 2026-09-13

没有普通用户时，用户页直接显示空状态，管理员账号看不见。

### 修复
- 用户列表始终显示管理员（套餐 / 流量为 —，不可订阅或删除）
- 搜索「管理员」或用户名也能找到

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.24`

## v0.1.24 — 2026-09-13

密码单独占一侧栏，设置页把面板、证书叠在一起，管理员也不能改自己的登录名。

### 改进
- 设置改为 Tab：面板 / 证书 / 账号
- 密码收进设置的账号 Tab，侧栏不再单独列出
- 登录用户可改用户名（含管理员）；改用户名或密码需填写当前密码
- 用户侧「修改密码」改为设置，同样可改用户名

### 升级注意
- 只升面板即可。旧地址 `/password`、`/certs` 会跳到对应 Tab
- 回滚：`liking-upgrade --release v0.1.23`

## v0.1.23 — 2026-09-13

证书单独占一侧栏，又只能粘贴 PEM，没有域名的人没法给 Trojan / XHTTP / AnyTLS 配 TLS。

### 改进
- 证书收进设置，侧栏不再单独列出
- 三种签发：Cloudflare DNS 申请 Let's Encrypt（支持泛域名，域名不必指向面板）、自签、上传 PEM
- Let's Encrypt 到期前约 30 天自动续期；设置里保存 Cloudflare Token 和 ACME 邮箱（Token 不明文回显）

### 升级注意
- 只升面板即可。已有上传的证书仍可用
- 回滚：`liking-upgrade --release v0.1.22`

## v0.1.22 — 2026-09-13

浅色主题是暖米色加 terracotta，看起来不像纯白管理台。

### 改进
- 默认改为高清白：白底、细灰线、黑字、黑色主按钮
- 侧栏选中、表格、徽章去掉暖色铺底
- 深色改为锌黑配对，右上角仍可切换

### 升级注意
- 只升面板即可。已手动选过深色的人仍走深色
- 回滚：`liking-upgrade --release v0.1.21`

## v0.1.21 — 2026-09-13

首次安装成功只提示打开浏览器，管理员密码写在 journal 里，终端看不见。自定义 `--bootstrap-admin-password` 也没真正传到首次启动。

### 改进
- 安装成功后打印面板地址、用户名 `admin` 和密码
- 未指定密码时由安装脚本生成，并在服务启动时传入
- 重置密码后同样打印新密码

### 修复
- 一次性 bootstrap drop-in 改为启动后再删除，自定义首装密码生效

### 升级注意
- 已安装面板不受影响。新机器直接跑一键安装即可
- 已装机器更新安装脚本：`liking-upgrade update-script`
- 回滚：`liking-upgrade --release v0.1.20`

## v0.1.20 — 2026-09-13

服务器卡片上看不出这台机器现在忙不忙、已经走了多少流量、还剩多少。

### 改进
- 每台服务器展示实时上行 / 下行，页面约 5 秒刷新
- 已用流量按这台机器上的线路累计；可设上限，并显示剩余
- 总览节点表同步显示上行、下行、已用

### 说明
- 新 Agent 报网卡速率（排除 lo / docker / veth 等）
- 旧 Agent 仍可用 Xray 用户统计估算实时速率
- 已用流量不依赖升 Agent

### 升级注意
- 升面板即可看到已用流量；要看网卡实时速率，请重新执行该节点的 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.19`

## v0.1.19 — 2026-09-13

用户行上挤着订阅、清流量、改密、续期和换套餐，没法改用户名，也不能单独设流量上限。停用时还会把自定义流量上限清掉。

### 改进
- 行操作改为「订阅 / 编辑 / 停用 / 删除」，套餐和到期在表格里只读
- 编辑弹窗可改用户名、备注、套餐、到期、流量上限、登录密码，并清零已用流量
- 到期可用日期选择，并提供 +30 / +60 / +90 天

### 修复
- 只改启用状态时不再清空自定义流量上限
- 同一套餐保存时不再清零已用流量（换套餐仍会清零）

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.18`

## v0.1.18 — 2026-09-13

侧栏「节点管理」和「添加线路」对不上常见面板的叫法；新建时名称预填 HK-8443、端口预填 8443，容易和已用口撞上。

### 改进
- 侧栏和页面改为「服务器管理」；每台机器上「增加节点」
- 名称默认留空，保存时按协议和端口生成
- 端口不填则在 10000–59999 随机，并错开该服务器已用端口

### 升级注意
- 只升面板即可
- 回滚：`liking-upgrade --release v0.1.17`

## v0.1.17 — 2026-09-13

SS2022 点「复制」提示「节点未填写公开地址」，Agent 其实已经上报了连接 IP。即使用户后来填了地址，旧的 `ss://` 还会把密钥里的 `/` `=` `:` 转成百分号，小火箭经常导不进去。线路也只听了 TCP。

### 修复
- 公开地址为空时，复制 / 订阅改用 Agent 上报的连接 IP；心跳时若公开地址仍空则自动填上（回环地址除外）
- SS2022 分享改为 SIP002：`ss://Base64URL(method:serverPSK:userPSK)@host:port#name`，密钥不再百分号编码
- SS2022 入站同时听 TCP 和 UDP

### 升级注意
- 只升面板即可。已在线的节点下次心跳会补上公开地址
- 套餐要勾选这条 SS2022 所在的节点，否则复制会提示还没有可用用户
- 回滚：`liking-upgrade --release v0.1.16`

## v0.1.16 — 2026-09-12

Mieru 会一会通、过一会又不通。节点没有公网 IPv6 时，mita 默认按 DNS 返回的第一个地址连；先返回 AAAA 就连不上，等解析顺序变了又通。面板每 30 秒重下发时还会对 Mieru 端口做一次空的 TCP 探测。

### 修复
- mita 配置固定 `dns.dualStack=PREFER_IPv4`，并设置 `mtu=1400`
- 配置没变且 mita 仍在 RUNNING 时不再 `stop`，也不再 TCP 拨测业务端口

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，只升面板不够
- 回滚：`liking-upgrade --release v0.1.15`

## v0.1.15 — 2026-09-12

Mieru 保存「TCP+UDP」时，占用检测把 mita 自己正在听的端口当成冲突，线路被自动停用，复制出来的端口上没人听。停用时还会删掉用户账号，再启用就换密码。

### 修复
- Agent 下发 mita 前先 `stop`，等自己的端口释放后再检测占用，改传输 / 改端口不会把线路自动关掉
- 下发后确认 mita 为 RUNNING，并用本机连接确认端口已在听
- 停用线路时保留该用户的 Mieru 用户名和密码，再启用不用重新导入

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，只升面板不够
- 已停用的 Mieru 线路请再启用一次，然后重新「复制」链接（若之前被自动停用，密码可能已换）
- 回滚：`liking-upgrade --release v0.1.14`

## v0.1.14 — 2026-09-12

Mieru 改端口后配置写了新口，mita 进程还在听旧口，客户端连的是空端口。小火箭默认走 UDP，线路却只开了 TCP。

### 修复
- Agent 下发 mita 配置后先 `stop` 再 `start`，端口和传输立刻生效
- 新建 Mieru 默认 TCP+UDP；分享链接在 UDP/BOTH 时为 `udp=1`（小火箭）
- Clash 订阅里 BOTH 按 UDP 导出

### 升级注意
- **必须重新执行节点上的 Agent 安装命令**，否则改端口仍不会重绑
- 已有 Mieru 线路请编辑一次传输为「BOTH」或「UDP」再保存
- 回滚：`liking-upgrade --release v0.1.13`

## v0.1.13 — 2026-09-12

线路「复制」被做成了订阅链接。小火箭认的是 `mierus://` 协议链接，例如 `mierus://user:pass@host?udp=1&port=39198&profile=default`。

### 修复
- 「复制」只复制协议链接：VLESS / Trojan / SS 仍是 `vless://` 等；Mieru 改为小火箭格式 `mierus://用户:密码@地址?udp=&port=&profile=default`
- 不再弹出订阅面板；复制失败时只展示这条协议链接

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.12`

## v0.1.12 — 2026-09-12

线路「复制」贴出的 `mieru://` 多数软件不认。真正通用的是 HTTP 订阅链接。

### 改进
- 点「复制」弹出导入面板，并自动复制通用订阅（`/api/sub/令牌`）：Clash / v2rayN / 小火箭 / sing-box 都能添加订阅
- 同时给出 Clash Meta 订阅；VLESS / Trojan / SS 仍可复制单节点 `vless://` 等
- Mieru / AnyTLS 不再把自定义单节点链接当作可导入格式，提示用 Clash Meta 订阅

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.11`

## v0.1.11 — 2026-09-12

节点管理里点「复制」，贴出来的是公钥、端口这类参数，不是 `vless://` 分享链接，代理软件导不进去。

### 修复
- 「复制」改为复制该线路一个可用用户的分享链接（`vless://` / `trojan://` / `ss://` / `anytls://` / `mieru://`）
- 没有用户或未填公开地址时给出明确提示；「参数」仍复制服务端字段，并说明不能当分享链接
- Mieru 单条链接多数客户端不认，提示改用用户页的 Clash 订阅

### 升级注意
- 只升面板即可，节点 Agent 不用重装
- 回滚：`liking-upgrade --release v0.1.10`

## v0.1.10 — 2026-09-12

节点只装了 Xray 时，创建 Mieru / AnyTLS 会被面板拦住，而且 Agent 也不会去装 mita。

### 修复
- 允许在尚未上报 mita / sing-box 时创建对应线路，配置会下发给节点
- Agent 第一次下发时从 GitHub 安装缺失内核（mita 3.36.1、sing-box 1.14.0、Xray），装好后回传已装内核
- 创建表单不再拦截，只提示「将自动安装」；装失败时节点卡片显示下发错误

### 升级注意
- 必须重新执行节点上的 Agent 安装命令，否则仍不会自动装 mita
- 节点需要能访问 GitHub；可设环境变量 `LIKING_GITHUB_PROXY`
- 回滚：`liking-upgrade --release v0.1.9`

## v0.1.9 — 2026-09-12

侧栏不再拆「服务器 / 入站」，改成一页「节点管理」：一台机器下面挂线路。

### 改进
- 新页面「节点管理」：节点卡片 + 嵌套线路表，在本机上添加/编辑/停用线路
- 侧栏「节点」分组只保留 节点管理、证书；旧地址 `/servers`、`/inbounds` 自动跳转
- 总览、套餐文案跟着改成「节点 / 线路」，避免再在两个菜单之间来回跳
- 添加线路时节点已锁定，端口仍可自定义（默认建议 8443，避开 443）

### 升级注意
- 面板 UI 变更，升到 v0.1.9 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.8`

## v0.1.8 — 2026-09-12

对照妙妙屋的操作面板，把 liking 管理端从金纸衬线风改成紧凑后台。

### 改进
- 侧栏分组（节点 / 业务 / 系统），列表优先，新建和编辑都在弹窗里完成
- 去掉 Cormorant 衬线与金纸底纹；Fira Sans 全文，terracotta 强调色，深浅色都可用
- 用户、套餐、入站、服务器、证书的创建表单移入对话框；空状态带主操作按钮
- 登录改为居中卡片；总览数字与套餐/服务器卡片改为信息密度更高的排版

### 升级注意
- 面板 UI 变更，升到 v0.1.8 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.7`

## v0.1.7 — 2026-09-12

对照妙妙屋的日常操作，把开户、续期、订阅和入站参数抄进 liking（不搬偷自己、Nginx、TG、十二套模板、一人多套餐）。

### 新增
- 创建用户可留空密码：服务端随机生成并返回，面板自动复制
- 用户续期 `extend_days`（+30 / +60 / +90），只改到期日，不清流量
- 用户搜索、按套餐筛选、流量进度条、到期标记、订阅弹窗（二维码 + Clash / sing-box 一键导入）
- 入站按服务器筛选；可查看/复制 REALITY 公钥、short_id、dest、SS2022 PSK 等参数（不含私钥）
- 套餐卡片展示流量、周期、关联节点和用户数；不勾选节点时二次确认「全部节点」
- 服务器改为卡片：在线状态、已装内核、入站条数、心跳、安装命令
- 总览增加用户数、套餐数、已用流量；「我的订阅」带流量条和导入按钮

### 升级注意
- 面板 UI 变更，升到 v0.1.7 即可；节点 Agent 可暂不重装
- 回滚：`liking-upgrade --release v0.1.6`

## v0.1.6 — 2026-09-12

真机验收：套餐无法选节点、入站默认 443 被占用会拖垮整机、HTTP 下复制订阅失败、用户无法改套餐。

### 修复
- 创建/编辑套餐改为勾选**节点**（服务器）；不选表示全部节点，节点上后加的线路自动进入套餐
- 入站端口默认为 8443，可自定义 1–65535，可编辑已有入站的端口/名称
- 某一端口被占用时只停用该入站，其它线路继续监听；缺失 mita / sing-box 不再让 Xray 下发失败
- Agent 上报已安装内核；订阅只包含节点上实际能跑的协议
- HTTP 打开面板时复制订阅/安装命令不再静默失败，并弹出可手动复制的链接
- 用户列表可改绑套餐；创建用户后直接给出订阅链接

### 改进
- 入站表单显示该节点已用端口，并按空闲口自动建议
- 占用端口的入站会自动停用，改端口后可再启用

### 升级注意
- 升到 v0.1.6 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.4`（会失去套餐选节点与端口修复）

## v0.1.5 — 2026-09-12

未单独打 GitHub tag，内容并入 v0.1.6。

### 修复
- 创建/编辑套餐改为勾选**节点**（服务器），不再只显示入站名称小标签
- 勾选节点后，该节点上后加的线路会自动进入套餐
- 套餐列表和开用户时能看到套餐绑了哪些节点

## v0.1.4 — 2026-09-12

真机验收：Xray Stats API 路由配成 freedom，自己连自己把 fd 打到九万。

### 修复
- API 入站改为路由到 `api` 模块，不再走 `freedom`（原先 `127.0.0.1:10085` 会环回，几十秒内撑爆 1.9G 机器）

### 升级注意
- 升到 v0.1.4 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.3`（需对应 Release 资产；不建议回滚，会再出现内存风暴）

## v0.1.3 — 2026-09-12

真机验收：Xray 流量采集把 1.9G 机器吃满。

### 修复
- `xray api statsquery` 加 4 秒超时，避免卡死的采集进程把 API 连接堆在 10085
- 流量采集改为 30 秒一次，且同时只跑一条
- 拉起 Xray / sing-box 时设置 `GOMEMLIMIT=256MiB`，避免 Go 堆 + 透明大页把整机换爆

### 升级注意
- 升到 v0.1.3 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.2`（需对应 Release 资产）

## v0.1.2 — 2026-09-12

真机验收：配置没变时不再每 30 秒重启 Xray。

### 修复
- 周期巡检下发若配置未变且内核仍在跑，则跳过重启（原先 VLESS 会每 30 秒闪断）
- 相同的下发失败只记一次日志，避免刷 journal
- 下发失败文案去掉多余的 `apply rejected:` 前缀
- Agent 安装时清理 v0.1.0 留在 `/var/lib/liking/xray.log` 的残留日志

### 升级注意
- 升到 v0.1.2 后请重新执行节点 Agent 安装命令
- 回滚：`liking-upgrade --release v0.1.1`（需对应 Release 资产）

## v0.1.1 — 2026-09-12

真机验收修复：配置下发失败可见、端口占用不再抢已有服务、Xray 日志不再刷盘。

### 修复
- 节点配置下发失败会写入服务器 `last_error`，总览和服务器页直接显示，不再只打日志
- 创建/修改入站会同步等待下发结果，失败时返回 `apply_error`
- Agent 启动前检测公网端口；若 443 等已被占用则拒绝下发并回滚到上一份可用配置（避免和本机其它服务抢端口）
- Xray 关闭 access 日志，避免 Stats API 把 `xray.log` 刷到数 MB
- 并发下发合并为每台机器一条队列，减少 Xray 反复重启和僵尸进程
- Agent 数据目录改为 `/var/lib/liking/agent`，不再和面板 `panel.db` 混在同一目录

### 改进
- 登录页与卡片质感加强；443 端口给出占用提示
- Agent systemd 带完整 PATH；重装安装脚本会 restart 而不是只 enable
- 面板 `install.sh` / `liking-upgrade` 替换二进制后会 `systemctl restart`（原先 `enable --now` 不会重启已在跑的进程）
- 某一内核缺失时仍下发其它内核（例如没有 mita 时 VLESS 照常监听）

### 升级注意
- 升到 v0.1.1 后请在节点上重新执行面板里的 Agent 安装命令，以更新二进制和数据目录
- 入站默认仍是 443；本机已有 Nginx / 其它面板占用 443 时请改用 8443
- 回滚：`liking-upgrade --release v0.1.0`（需对应 Release 资产）

## v0.1.0 — 2026-09-12

第一版面板：主控 + 节点 Agent，深色金质管理台，一键安装与升级。

### 新增
- 管理员登录、服务器列表、反向 WSS 纳管节点
- 七组入站：VLESS+REALITY、VLESS+REALITY+Vision、VLESS+XHTTP+TLS、Trojan+TLS、SS2022、AnyTLS+TLS、Mieru
- 用户 / 套餐（一人一套餐）；到期或超量从内核配置摘掉客户端
- 订阅：Clash Meta YAML、sing-box JSON、URI（base64）
- 链式转发：入口 → 另一台落地；Mieru / AnyTLS 不能当链式落地
- 面板一键安装、`liking-upgrade` 升级、`liking-uninstall` 卸载；支持 `--from-dir` 离线安装
- 节点从面板 `/v1/install-agent` 下载 Agent，不走 GitHub；`/v1/agent-bin` 带 `X-SHA256`，支持 HEAD

### 改进
- 默认深色电影金质 UI，浅色为暖纸色例外主题；字体打进二进制，不依赖 Google Fonts
- SQLite WAL；删除服务器会断开在线 Agent
- 列表接口不再返回节点 token

### 升级注意
- 默认监听 `:8899`
- 节点需自行安装内核到 PATH：`xray`、AnyTLS 用 `sing-box`（≥1.12）、Mieru 用 `mita`
- 首次安装会打印 admin 密码；也可用 `--bootstrap-admin-password`
